import { Unit } from ".";

export class Player extends Unit {
    constructor(health: number, max_health: number, movement_speed: number) {
        super(health, max_health, movement_speed);
        
    }

    
}